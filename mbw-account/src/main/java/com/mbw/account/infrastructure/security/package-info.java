/**
 * Security primitives for the account module: BCrypt password hashing
 * and Nimbus JOSE JWT issuance. The
 * {@code com.mbw.account.domain.service} package owns the pure
 * interfaces; this package owns the framework-coupled implementations
 * + Spring {@code @ConfigurationProperties} bindings.
 */
package com.mbw.account.infrastructure.security;
