/**
 * Repository contracts owned by the domain layer.
 *
 * <p>Pure interfaces; framework-free per project convention. JPA /
 * Redis adapters live in
 * {@code com.mbw.account.infrastructure.persistence}, implementing
 * these interfaces and adapting persistence types back to domain
 * types.
 */
package com.mbw.account.domain.repository;
