/**
 * Account module — user identity, credentials, sessions, lifecycle state machine.
 *
 * <p>Spring Modulith auto-detects this package as an application module (direct subpackage
 * of {@code com.mbw} where {@code MbwApplication} resides). No explicit {@code @ApplicationModule}
 * annotation needed at this stage.
 *
 * <p>Module spec: see {@code spec/account/README.md} (sibling of this repo's pom.xml).
 */
package com.mbw.account;
