/**
 * Shared kernel: cross-module error codes, base utilities, event contracts, domain primitives.
 *
 * <p>Currently empty (placeholder package-info only); first concrete types land in PR-3+ as
 * use cases discover cross-module needs.
 */
package com.mbw.shared;
